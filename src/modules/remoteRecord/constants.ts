export const namespace = 'remoteRecord';

export const FETCH_DECODERS = `${namespace}/FETCH_DECODERS`;

export const RECORD = `${namespace}/REQUEST_RECORD`;

export const RESET_REMOTE_RECORD = `${namespace}/RESET_REMOTE_RECORD`;
