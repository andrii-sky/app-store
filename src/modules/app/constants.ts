export const namespace = 'app';

export const RESET_STORE = `${namespace}/RESET_STORE`;

export const REHYDRATE_STORE = `${namespace}/REHYDRATE_STORE`;

export const SET_NAVIGATION_READY = `${namespace}/SET_NAVIGATION_READY`;

export const SET_REALM_SYNCED = `${namespace}/SET_REALM_SYNCED`;

export const SET_EPG_LOADED = `${namespace}/SET_EPG_LOADED`;
