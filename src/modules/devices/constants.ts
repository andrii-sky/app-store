export const namespace = 'devices';

export const REGISTER_DEVICE = `${namespace}/REGISTER_DEVICE`;

export const DEACTIVATE_DEVICE = `${namespace}/DEACTIVATE_DEVICE`;

export const FETCH_DEVICES = `${namespace}/FETCH_DEVICES`;

export const UPDATE_DEVICE_NAME = `${namespace}/UPDATE_DEVICE_NAME`;

export const SET_PLAYBACK_DEVICE = `${namespace}/SET_PLAYBACK_DEVICE`;

export const SET_SERIAL_NUMBER = `${namespace}/SET_SERIAL_NUMBER`;
