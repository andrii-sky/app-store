// eslint-disable-next-line import/prefer-default-export
export enum BrandType {
  Movie = 'Movie',
  Show = 'Show',
  Episode = 'Episode',
  // for compatibility, when use an id of show episode in detail page
  ShowEpisode = 'episode',
  Channel = 'Channel',
  Collection = 'Collection',
}
