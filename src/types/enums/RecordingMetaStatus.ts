// eslint-disable-next-line import/prefer-default-export
export enum RecordingMetaStatus {
  SCHEDULED = 'SCHEDULED',
  ACTIVE = 'ACTIVE',
  PARTIAL = 'PARTIAL',
  DONE = 'DONE',
  DONE_ERROR = 'DONE_ERROR',
}
